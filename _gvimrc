" Valuable definition
let s:vimdir = $HOME . '/.vim'
let s:bundledir = s:vimdir . '/bundle/'
set runtimepath+=$HOME/.vim/bundle/molokai


" Color setting
" set background=dark
" colorscheme hybrid
" colorscheme molokai
" colorscheme badwolf
colorscheme solarized

set t_Co=256
" let g:molokai_original = 1
let g:rehash256 = 1

if has('gui_running')
    set background=dark
else
    set background=light
endif
